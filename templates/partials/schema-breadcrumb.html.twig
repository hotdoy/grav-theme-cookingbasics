{% set trail = '{}' %}
{% set options = { items:{'@self.parent':''} } %}
{# {% set collection = page.collection(options) %} #}


{% for a in page.collection(options) if a.routable %}

  {% set trail = trail|merge({}) %}

  {% for b in a.collection(options) if b.routable %}

  {{b.title}}

  {% endfor %}

{% endfor %}


{{ trail }}










<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [{
    "@type": "ListItem",
    "position": 1,
    "name": "Books",
    "item": "https://example.com/books"
  },{
    "@type": "ListItem",
    "position": 2,
    "name": "Science Fiction",
    "item": "https://example.com/books/sciencefiction"
  },{
    "@type": "ListItem",
    "position": 3,
    "name": "Award Winners"
  }]
}
</script>

